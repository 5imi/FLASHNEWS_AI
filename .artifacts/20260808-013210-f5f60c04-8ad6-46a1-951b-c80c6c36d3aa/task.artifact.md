# Task Management

- [ ] **Step 1: AI Chat Remediation**
    - [ ] Add `GROQ_API_KEY` and `OPENROUTER_API_KEY` to `app/build.gradle.kts`
    - [ ] Update `AiClient.kt` to refine failover logic
    - [ ] Update `GeminiClient.kt` to increase context limit (1500 chars)
    - [ ] Update `GrokClient.kt` to increase context limit (1500 chars)
    - [ ] Update `LocalAiClient.kt` for better Romanian heuristics
    - [ ] Update `FeedViewModel.kt` to fix `AiOrchestrator` construction and use all keys
    - [ ] Update `SearchViewModel.kt` to fix `AiOrchestrator` construction and use all keys
- [ ] **Step 2: Notification Deep Linking**
    - [ ] Verify `NewsSyncWorker.kt` Intent extras
    - [ ] Update `MainActivity.kt` to handle `article_url` in `onCreate` and `onNewIntent`
- [ ] **Step 3: Concurrent RSS Sync**
    - [ ] Refactor `RssClient.kt` to use parallel coroutines and `Semaphore`
- [ ] **Step 4: Persistence & Deduplication**
    - [ ] Increment `NewsDatabase.kt` version (if needed, though fields exist)
    - [ ] Ensure `NewsRepository.kt` uses stored AI results and title deduplication correctly
- [ ] **Step 5: Security & UI**
    - [ ] Broaden `network_security_config.xml` if necessary
    - [ ] Verify `EmptyState` usage
- [ ] **Final Verification**
    - [ ] Run `.\gradlew.bat assembleDebug`
